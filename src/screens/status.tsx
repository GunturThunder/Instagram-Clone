import React from 'react'
import { View, Text, SafeAreaView, StatusBar } from 'react-native'

export const StatusScreen = (): React.ReactElement => {
    return (
       <SafeAreaView>
           <Text>Status</Text>
       </SafeAreaView>
    )
}