import React from 'react'
import { View, Text, SafeAreaView, StatusBar } from 'react-native'

export const CallsScreen = (): React.ReactElement => {
    return (
       <SafeAreaView>
           <Text>Calls</Text>
       </SafeAreaView>
    )
}